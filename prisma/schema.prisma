// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model PriceRecord {
  id        String   @id @default(cuid())
  symbol    String   // "GOLD_EGP" or "USD_EGP"
  price     Float
  currency  String   // "EGP"
  change    Float?   // percentage change from previous
  source    String?  // Where the price was fetched from
  buyPrice  Float?   // Price the shop buys from you (for gold)
  sellPrice Float?   // Price the shop sells to you (for gold)
  createdAt DateTime @default(now())
}

model AppConfig {
  id    String @id @default(cuid())
  key   String @unique
  value String
}

model InvestmentPlan {
  id             String  @id @default(cuid())
  priceRangeMin  Float
  priceRangeMax  Float?  // null means > priceRangeMin (e.g., > 7500)
  action         String  // "شراء" (Buy), "بيع 70%" (Sell 70%), "لا شراء" (No Buy), etc.
  expectedReturn Float   // percentage (e.g., 40, 30, 0, -30, -50)
  label          String  // description in Arabic
  order          Int     // display order
  active         Boolean @default(true)
}

model NotificationLog {
  id        String   @id @default(cuid())
  type      String   // "daily_report", "buy_signal", "usd_drop_alert", "test"
  title     String
  message   String
  sentAt    DateTime @default(now())
  success   Boolean
  error     String?
}

model TelegramUser {
  id          String   @id @default(cuid())
  name        String   // Display name for the user
  botToken    String   // Unique Telegram Bot Token for this user
  chatId      String   // Unique Chat ID for this user
  active      Boolean  @default(true) // Whether to send notifications to this user
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
